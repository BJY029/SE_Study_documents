**OSI(Open Systems Interconnection) Model**
- 네트워크 통신 기능을 7개의 계층으로 나누는 프레임워크.
-  상위 계층의 기술은 하위 계층의 동작 방식을 잘 몰라도 사용할 수 있기에 추상화의 이점을 누릴 수 있다.
- OSI 모델의 7개 계층은 다음과 같이 구분 가능하다.
	- 물리 계층(Physical Layer): 제 1 계층
		- 물리적 통신 매체와 해당 매체를 통해 데이터를 전송하는 기술
		- 광섬유 케이블, 구리 케이블, 공기와 같은 다양한 물리적 채널을 통해 디지털/아날로그 신호를 전송한다.
	- 데이터 링크 계층(Data Link Layer): 제 2 계층
		- 이미 존재하는 네트워크를 통해 두 시스템을 연결하는 데 사용되는 기술
		- 상위 계층에서 받은 데이터 패킷을 프레임으로 나누어 관리한다.
		- 주된 역할은 데이터의 흐름 제어 및 오류 제어이다.
	- 네트워크 계층(Network Layer): 제 3 계층
		- 서로 다른 분산 된 네트워크 간의 데이터 전송을 용이하게 하는 역할 수행
		- 상위 계층의 세그먼트를 패킷으로 세분화하여 관리한다.
		- 주된 역할은 데이터가 표적에 도달하기 위한 최상의 물리적 경로를 찾는 '라우팅'이다.
		- 사용되는 프로토콜은 IP, ICMP, IGMP, IPsec 등이 있다.
	- 전송 계층(Transport LayeR): 제 4 계층
		- 두 기기(host)간의 종단 간 통신을 담당한다.
		- 상위 계층의 데이터를 세그먼트로 분할하여 관리한다.
		- 주된 역할은 흐름 제어 및 오류 제어이다.
			- 데이터를 읽는 속도보다 보내는 속도가 빨라서 오버 플로우가 발생하는 것을 방지하기 위한 흐름 제어를 수행한다.
			- 또한 데이터 패킷 자체의 오류나 데이터 패킷의 손실을 인지하고 재전송을 하는 방식으로 오류 제어를 수행한다.
		- 사용되는 대표적인 프로토콜은 TCP와 UPD가 있다.
	- 세션 계층(Session Layer): 제 5 계층
		- 두 기기 사이의 통신을 시작하고 종료하는 일을 담당하는 계층이다.
		- 통신이 시작될 때부터 종료될 때까지의 시간을 세션이라고 한다.
		- 세션 설정, 대화 제어, 세션 종료와 같은 역할을 수행한다.
		- 동기화 및 체크포인트 기능 또한 제공한다.
			- 긴 데이터 전송 간 중단이 발생하면, 세션을 처음부터 다시 하지 않고, 설정된 체크포인트부터 세션을 재개한다.
	- 프레젠테이션 계층(Presentation Layer): 제 6 계층
		- 서로 다른 시스템을 사용하는 경우 이를 상위 계층이 이해할 수 있는 구문으로 데이터를 변환하는 일을 담당하는 계층이다.
		- 데이터 표현, 압축 및 복원, 암호화 및 복호화, 직렬화 및 역직렬화와 같은 역할을 수행한다
	- 애플리케이션 계층(Application Layer): 제 7 계층
		- 사용자의 데이터와 직접 상호 작용하는 유일한 계층으로, 소프트웨어는 통신을 개시하기 위해 애플리케이션 계층에 의지한다.
		- 즉, 어떤 응용 프로그램이 어떤 서비스에 접근하고, 어떤 프로토콜을 쓰고, 어떤 방식으로 데이터를 주고 받을지 등에 대해 정의한다.
		- HTTP, SMTP, DNS와 같은 프로토콜이 해당 계층에서 동작한다.

**TCP/IP Model**
- TCP/IP는 특정 통신 문제를 해결하고 표준화 된 프로토콜에 의존하는 실용적 모델로 5개의 계층으로 구성된다.(4개의 계층으로 설명되기도 한다)
- ![[Pasted image 20251112181546.png]]
- 각 계층 설명은 다음과 같다.
	- 물리적 네트워크 계층
		- 프레임 내부의 각 비트를 한 노드에서 다음 노드로 이동하는 기능을 수행한다.
		- 해당 계층의 프로토콜은 링크와 링크의 실제 전송 매체(꼬임쌍선, 광케이블 등)에 의존한다.
	- 데이터 링크 계층
		- 같은 링크 상의 다른 장치간에 데이터를 주고 받는 기능을 수행한다.
		- 오류 제어 및 프레이밍을 제공한다.
		- 대표적인 프로토콜은 이더넷, 와이파이, DOCSIS 등이 있다.
	- 인터넷 계층(네트워크 계층, IP 계층)
		- 한 호스트에서 다른 호스트로 데이터그램을 라우팅하는 책임을 진다.
		- 데이터그램의 각 필드를 정의하는 IP 프로토콜과, 데이터그램의 이동 경로를 결정하는 라우팅 프로토콜을 포함한다.
	- 전송 계층
		- 패킷이 손실되지 않고 오류 없이 도착 하도록 하며, 종단간 통신을 담당한다.
		- 대표적인 프로토콜로 TCP와 UDP가 있다.
	- 응용 프로그램 계층
		- 누구나 사용할 수 있는 표준 인터넷 서비스 및 네트워크 응용 프로그램을 정의한다.
		- HTTP, SMTP, FPT와 같은 많은 프로토콜을 제공한다.

**IP Addressing**
- Internet Protocol(IP) 주소는 인터넷에 연결된 모든 장치에 할당된 고유 식별 번호다
- 공용, 개인, 정적 및 동적 IP 주소가 존재한다.
- IP주소를 표기하는 두 가지 표준 버전이 존재한다.
	- IPv4
		- IP의 네 번째 버전으로 32bit 이진수 번호를 사용하여 고유한 IP 주소를 구성한다.
		- 4개의 숫자 집합 형식을 취하기 때문에, 각 숫자는 0~255 범위이다.
			- (ex : 185.107.80.42)
		- 폭발적으로 늘어나는 인터넷 장치로 인해 주소 고갈 문제가 발생했다.
		- IP 주소 클래스
			- 클래스 A 네트워크: 첫 번째 마침표 이전의 모든 항목이 네트워크를 나타냄
				- (ex: 203.0.113.112 →네트워크:203, 장치: 0.113.112)
			- 클래스 B 네트워크: 두 번째 마침표 이전의 모든 항목이 네트워크를 나타냄
				-  (ex: 203.0.113.112 →네트워크:203,.0 장치: 113.112)
			- 클래스 C 네트워크: 세 번째 마침표 이전의 모든 항목이 네트워크를 나타냄
				-  (ex: 203.0.113.112 →네트워크:203.0.113, 장치: 112)
	- IPv6
		- 128 비트 주소 체계를 사용하여 IPv4의 주소 고갈 문제를 해결했다.
		- 4개의 16진수 숫자가 포함된 8개의 그룹으로 구성된다.
			- (ex: 2001 : 0DC8 : E004 : 0001 : 0000 : 0000 : 0000 : F00A)
- 서브넷 마스크
	- 네트워크 내에서 내부적으로 사용되며, 주어진 IP 주소를 네트워크 환경에 맞게 네트워크와 장치로 구분하기 위한 32비트 값이다.
	- 32비트에서 네트워크 영역은 1, 장치 영역은 0으로 설정된다.
	- ex) IP 주소: 192.168.1.10, 서브넷 마스크: 255.255.255.0 -> 장치 주소: 10
- DHCP(동적 호스트 구성 프로토콜)
	- 조직의 네트워크에 있는 각 호스트에게 IP 주소를 동적으로 할당하는 데 사용한다.
	- DHCP의 구성요소는 다음과 같다.
		- DHCP 서버 : 네트워크에 연결하려는 장치에 IP 주소와 추가 네트워크 매개변수를 자동으로 제공하는 데 사용한다.(공유기, 리눅스 서버 등)
		- DHCP 클라이언트: PC, 프린터와 같은 IP 주소를 요청하는 장치가 해당된다.
		- DHCP 릴레이: 다른 네트워크 대역에 있는 클라이언트의 요청을 DHCP 서버로 전달
	- DHCP 동작 방식은 다음과 같다.
		- Discover: DHCP 클라이언트는 DHCP 서버를 찾기 위해 해당 메시지를 브로드캐스트로 전송
		- Offer: Discover를 수신한 DHCP 서버는 해당 클라이언트에 할당할 IP 주소, 서브넷, 게이트웨이, DNS 정보 등을 포함한 DHCP 메시지를 클라이언트로 전송한다.
		- Request: DHCP 서버로부터 제안받은 IP주소와 서버 정보를 포함한 DHCP 요청 메시지를 브로드캐스트로 전송한다.
		- Acknowledgement: 해당 IP 주소를 사용하겠다는 요청을 받은 DHCP 서버는 해당 IP를 어떤 클라이언트가 언제부터 사용하기 시작했는지 정보를 기록하고, Request 메시지를 정상적으로 수신했다는 응답을 전송한다.
- Scheduling
	- ![[Pasted image 20251112201435.png]]
	- 네트워크 엔지니어가 네트워크 장치 간에 IP 주소를 할당하는 방법을 나타내기 위해 준비하는 문서 
	- 이는 주소 중복 방지 및 확장성과 관리 용이성을 위해 필요한 절차이다.
	- 다음과 같은 구성요소가  포함된다.
		- Address space: 네트워크에서 사용 가능한 전체 IP 주소 범위
		- Subnet Mask: 주소 공간을 서브넷으로 분할하기 위한 비트 구분
		- Subnetting: 큰 네트워크를 여러 개의 작은 서브넷으로 나누는 과정
		- Address Allocation: 장비에 IP를 배정하는 방식
		- Naming Convention: 장비나 서브넷 명명 규칙

**Protocols**
- HTTP/HTTPS
	- 애플리케이션 계층에서 사용되는 프로토콜 중 하나로 웹 브라우저와 웹 서버 간 데이터를 주고 받는 통신 규약이다.
	- HTTPS는 HTTP에 보안 계층(SSL/TLS)을 추가한 통신 규약이다.
	- HTTP는 TCP를 전송 프로토콜로 사용하며, 지속 연결과 비지속 연결 방식으로 나뉜다.
		- 비지속 연결(Non-persistent HTTP) 동작 방식
			- TCP 연결 → 객체 전송 → TCP 연결 해제
			- 하나의 객체를 전송할 때 마다 위와 같은 과정을 반복적으로 수행한다.
		- 지속 연결(persistent HTTP) 동작 방식
			- TCP 연결 → 객체 전송 1 → 객체 전송 2 → ... → TCP 연결 해제
			- TCP 연결을 끊지 않고 지속적으로 유지하여연속적으로 객체를 전송 가능하다
- FTP
	- 컴퓨터 간에 파일을 전송하기 위한 애플리케이션 계층에서 사용되는 프로토콜이다.
	- FTP는 서로 통신하려는 컴퓨터를 연결하는 두 개의 연결을 여는 방식으로 작동하며, 하나의 연결은 두 클라이언트 간의 전송되는 명령과 답글을, 다른 하나의 연결은 데이터 전송을 처리한다.
	- FTP의 전송 중에 사용 하는 명령어 '보내기', '받기', '디렉토리 변경', '전송'이 있다.
	- FTP는 파일을 전송하는 동안 다음 세 가지 모드를 사용한다.
		- 스트림 모드: FTP가 데이터 문자열의 정보를 경계없이 관리할 수 있다.
		- 차단 모드: 데이터를 블록으로 분리한다.
		- 압축 모드: Lempel-Ziv라는 알고리즘을 사용하여 데이터를 압축한다.
- SMTP
	- SMTP는 인터넷 전자메일을 위한 주요 애플리케이션 계층 프로토콜이다.
	- SMTP는 Push 방식으로, 메시지를 보내는 쪽이 주도하여 연결을 시작하고 데이터를 전송하는 성격을 지닌다.
	- SMTP는 TCP를 사용하여 데이터를 전송하며, 메일 서버는 SMTP의 클라이언트로 동작하면서 동시에 서버로도 동작 가능하다.
	- 데이터 전송 간에 다른 곳을 거치지 않고 송신 서버에서 수신 서버로 바로 전달된다는 특징이 있다.
- DNS
	- DNS는 IP 주소와 host name을 매핑해주는 역할을 수행한다.
	- DNS는 분산 계층적으로 구조화되어 있다.
		- Root DNS Server: TLD 서버의 IP 주소를 제공한다.
		- TLD(Top Level Domain) server: Authoritative DNS 서버의 IP 주소를 제공한다.
		- Authoritative DNS server: 요청한 host name의 IP 주소를 제공한다.
	- 이러한 DNS는 보통 Local DNS server를 통해 접속하며, 해당 Local DNS server에서 반복적/재귀적 질의를 통해 IP 주소를 얻어온다.
- TCP
	- TCP는 트랜스포트 계층에서 사용되는 대표적인 프로토콜 중 하나로 신뢰적인 데이터 전송을 제공하는 프로토콜이다.
	- TCP의 특징으로는 다음이 있다.
		- 3-way handshaking을 통해 안정적인 연결 환경을 설정한다.
		- 패킷 오류와 패킷 손실을 checksum과 timer를 통해 인지하고 처리한다.
		- 수신자의 버퍼 크기를 고려하여 송신자의 전송 속도를 조절하는 flow control를 제공한다.
		- 수신자와 송신자 사이의 경로 혼잡도를 파악하여 congenstion control을 수행한다.
- UDP 
	- UDP 또한 트랜스포트 계층에서 사용되는 대표적인 프로토콜 중 하나로 간단하고 빠른 전송을 제공하는 프로토콜이다.
	- UDP의 특징으로는 다음이 있다.
		- checksum을 통해 간단한 패킷 오류를 잡아준다.
		- no-handshaking이므로 빠른 연결이 가능하다.
		- flow/congenstion control을 지원하지 않기 때문에 최소 전송률 보장이 가능하다.
	- 이러한 UDP는 보통 어느정도의 패킷 손실이 상관 없고, 최소 전송률 보장이 필요한 곳(실시간 멀티 게임, 실시간 스트리밍)에서 종종 사용된다.
- WebSocket
	- WebSocket은 애플리케이션 계층에서 사용되는 프로토콜로 TCP 위에서 동작한다.
	- 클라이언트와 서버 간의 양방향 통신을 가능하게 하며, 클라이언트와 서버 간에 실시간 데이터 전송이 가능하다.
	- WebSocket은 HTTP 기반 handshake가 수행된 후, 성공 시 TCP 연결을 사용하여 HTTP에서 WebSocket으로 업그레이드 된다.

게임 관련 용어
- Ping
	- Packet InterNet Groper는 ICMP를 기반으로 하는 인터넷 및 네트워크 문제를 진단하는 데 가장 널리 사용되는 도구 중 하나다.
	- Ping은 네트워크의 지정된 장치에 ICMP 에코 요청을 보내고 응답을 기다리는 방식으로 작동한다. 이때, 각 응답에는 수신된 바이트 수와 왕복 시간(RTT), TTL에 대한 정보가 제공된다.
	- 이러한 Ping 반환값을 통해 사용자는 연결 및 성능 문제를 해결하는데 사용한다.
- RTT
	- RTT는 네트워크 요청이 시작점에서 목적지로 갔다가 다시 시작점으로 되돌아 오는 데 걸리는 시간이다. 즉, 왕복 시간이다.
	- RTT를 통해 네트워크 관리자는 네트워크 연결의 속도와 안정성을 진단하는 데 사용한다.
	- ![[Pasted image 20251112221323.png]]
- Latency
	- Latency는 데이터가 네트워크의 한 지점에서 다른 지점으로 전달되는 데 걸리는 시간이다.
	- 이러한 Latency는 클라이언트 장치와 데이터 센터 간에 측정되며, 해당 값을 통해 개발자는 웹 페이지나 애플리케이션이 사용자에게 로드되는 속도를 이해할 수 있다.
	- Latency가 발생되는 주요 요인은 클라이언트와 서버 간의 거리이며, 통과해야 하는 네트워크 수가 많아질수록 Latency 확률이 높아진다.
	- 이를 해결하기 위해 사용되는 대표적인 방법 중 하나는 CDN를 사용하는 것이다.
- Packet Loss
	- 패킷 손실이 발생하는 주된 요인은 라우터 내의 버퍼(큐)가 꽉 차서 버려지게 되어 발생된다.
	- 이러한 패킷 손실을 막기 위해서 기존 노드에서 패킷이 재전송 될 수 있다.

Network devices
- Router
	- 라우터는 둘 이상의 패킷 전환 네트워크 혹은 서브 네트워크를 연결하는 장치이다.
	- 데이터 패킷을 목표 IP 주소로 전달하면서 네트워크 간의 트래픽을 관리하고, 여러 장치가 동일한 인터넷 연결을 사용할 수 있도록 한다.
	- 대부분의 라우터는 근거리 통신망(LAN)과 광역 네트워크(WAN)간에 데이터를 전달한다.
- Switch
	- 스위치는 라우터와 달리 여러 장치의 네트워크가 아닌 단일 장치로만 데이터를 전송한다.
		- 즉, 스위치는 장치를 상호 연결하는 데만 사용한다.
	- ![네트워크 트래픽은 인터넷에서 라우터로, 네트워크 스위치에서 컴퓨터로 이동합니다](https://cf-assets.www.cloudflare.com/slt3lc6tev37/6ENfwtM3iUH99JpYoEC9FY/04abc1654ceff2645f50713394ebcb73/network-switch.svg)
  - Modem
	  - 모뎀은 컴퓨터, 스마트폰, 태블릿 및 기타 장치를 인터넷에 연결할 수 있게 해주는 장치이다.
	  - 주된 역할은 장치의 디지털 신호를 아날로그 신호로 변환하거나, 반대로 수신되는 아날로그 신호를 디지털 신호로 변환한다.

Network Architecture
![[Pasted image 20251112223122.png]]
- PAN(Personal Area Network)
	- 매우 가까운 거리에 있는 장치들끼리 통신하는 네트워크다.
	- 스마트폰, 태블릿, 웨어러블 기기 등이 포함된다.
	- 매우 작은 규모이기 때문에 장거리 통신이나 많은 장치 연결에는 부적절하다.
- LAN(Local Area Network)
	- 비교적 제한된 지리적 범위 안에서 여러 장치가 서로 연결되는 네트워크.
	- 스위치, 라우터, 이더넷 케이블 등이 사용된다.
	- 데이터 전송 속도가 빠르고 관리/운영이 비교적 쉽다.
	- LAN을 넓게 구성하여 여러 건물이나 캠퍼스를 연결할 경우 MAN 수준으로 올라간다.
- MAN(Metropolitan Area Network)
	- 대개 도시 혹은 대형 캠퍼스 영역을 커버하는 네트워크이다.
	- 여러 LAN들을 연결하는 중간 규모 네트워크로 볼 수 있다.
	- 광섬유 같은 고속 전송 기술이 자주 사용되며, LAN 보다 설계 및 운영이 복잡하다.
- WAN(Wide Area Network)
	- 도시, 국가, 대륙 간의 영역을 커버하는 네트워크로 인터넷이 해당된다.
	- 여러 LAN 혹은 MAN이 서로 연결되어 구성되며, 전송 속도, 비용, 지연 등의 측면에서 제한이 있을 수 있다.


reference
- https://aws.amazon.com/ko/what-is/osi-model/
- https://www.cloudflare.com/ko-kr/learning/ddos/glossary/open-systems-interconnection-model-osi/
- https://www.fortinet.com/kr/resources/cyberglossary/tcp-ip-model-vs-osi-model
- https://docs.oracle.com/cd/E38901_01/html/E38894/ipov-6.html#scrolltoc
- Computer Networking: A Top-Down Approach | James Kurose
- https://www.fortinet.com/kr/resources/cyberglossary/what-is-ip-address
- https://www.cloudflare.com/ko-kr/learning/network-layer/what-is-a-subnet/
- https://www.fortinet.com/kr/resources/cyberglossary/dynamic-host-configuration-protocol-dhcp
- https://thebook.io/007046/0109/
- https://automationforum.co/ip-address-schedule/
- https://www.fortinet.com/kr/resources/cyberglossary/file-transfer-protocol-ftp-meaning
- https://learn.microsoft.com/ko-kr/azure/application-gateway/for-containers/websockets
- https://www.techtarget.com/searchnetworking/definition/ping
- https://www.cloudflare.com/ko-kr/learning/cdn/glossary/round-trip-time-rtt/
- https://www.cloudflare.com/ko-kr/learning/performance/glossary/what-is-latency/
- https://www.cloudflare.com/ko-kr/learning/network-layer/what-is-a-router/
- https://www.cloudflare.com/ko-kr/learning/network-layer/what-is-a-network-switch/
- https://www.lenovo.com/kr/ko/glossary/what-is-modem/?orgRef=https%253A%252F%252Fwww.google.com%252F&srsltid=AfmBOooZbdr_VJz4zjuW0eoFfBz6zyqBdO6Ns5I6GUYz4ZMpnxJn25ll
- https://bytebytego.com/guides/what-are-the-differences-between-wan-lan-pan-and-man/)